// Optional test skeleton - enable once you add test runner (vitest/jest) and mock API
/**
import { render, screen } from '@testing-library/react';
import CustomerSignup from './CustomerSignup';

test('renders signup form', () => {
  render(<CustomerSignup />);
  expect(screen.getByText(/Join a Store/i)).toBeInTheDocument();
  expect(screen.getByPlaceholderText(/Store Slug/i)).toBeInTheDocument();
});
*/
